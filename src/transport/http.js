/**
 * HTTP transport layer — REST-compatible endpoints.
 * See: .idea/newsy0.1.md §7
 */

export function createHttpTransport(app) {
  // TODO: Register HTTP routes
  throw new Error('Not implemented');
}
