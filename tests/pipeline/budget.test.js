/**
 * Budget selector tests — placeholder for development kickoff.
 */

import { describe, it, expect } from 'vitest';

describe('budget selector', () => {
  it.todo('should not exceed maxReadTimeMin');
  it.todo('should not exceed maxArousalLoad');
  it.todo('should enforce max 1 high-arousal cluster in non-deep mode');
  it.todo('should produce deterministic output for same input');
  it.todo('should prefer larger clusters');
});
